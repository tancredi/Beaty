loop-display-size = 150px

.board
    text-align center
    margin 20px 0

    > div
        display inline-block
        margin 20px

loop-display(color)
    circle(loop-display-size, color)
    font-size 30px
    font-weight 100
    no-select()

    &:after
        content ''
        display inline-block

.loop
    loop-display(color-primary)

    &.muted
        loop-display(rgba(color-primary, .2))

.recorder
    loop-display(rgba(color-primary, .5))
    transition background .2s

    &.recording
        background color-red

    &.preparing
        background color-yellow

.stats-bar
    debullet()
    text-align center
    margin-top 40px

    li
        display inline-block
        margin 0 20px

    input.signature
        width 7px

    input.tempo
        width 25px
        text-align center